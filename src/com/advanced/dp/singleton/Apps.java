package com.advanced.dp.singleton;

public class Apps {
public static void main(String[] args) {
	Normal obj = new Normal();
	obj.print();
	/*Singleton object = new Singleton(); */                     /*here singleton object is not created because ,
	                                                           we are allowed to create only one object
	                                                            that's why it is singleton,so we keep its
	                                                             access modifier as private*/
	                                                         
}
}
